apiVersion: v1
kind: Service
metadata:
  name: dev-svc-graphql
  namespace: dev
  labels:
    name: dev-svc-graphql
    tier: backend
spec:
  type: NodePort
  ports:
    - port: 8000
      protocol: TCP
  selector:
    name: dev-pod-graphql
    tier: backend
