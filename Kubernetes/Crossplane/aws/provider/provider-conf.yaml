apiVersion: aws.upbound.io/v1beta1
kind: ProviderConfig
metadata:
  name: andromeda-providerconf
  labels:
    name: andromeda-providerconf
    type: provider-conf
    project: andromeda
spec:
  credentials:
    source: Secret
    secretRef:
      namespace: crossplane-system
      name: aws-secret
      key: creds
